import React from 'react';

import { Container, CustomText, CustomLink } from './styles';

export function TextFooter() {

  return (
    <Container> 
      <CustomText>Do not have an account yet?</CustomText>
      <CustomLink>Sign Up</CustomLink>
    </Container>
    
  )
};